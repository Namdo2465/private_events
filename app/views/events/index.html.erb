<h1>Upcoming Events</h1>

<% @events.each do |event| %>
    <div>
      <h2><%= event.name %></h2>
  
      <p>
        <%= event.location %>
      </p>
  
      <p>
        <%= event.date %>
      </p>
    </div>
  <% end %>